interface:
  display_name: "InterviewPilot Media Runtime"
  short_description: "Handle Mimo ASR, TTS, realtime transcription, and WebSocket push flows."
  default_prompt: "Use $interview-pilot-media-domain to analyze or modify realtime transcription, WebSocket, or TTS logic."

policy:
  allow_implicit_invocation: true
